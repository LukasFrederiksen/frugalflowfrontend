const DetailSectionDescription = ({ title, detail }) => (
  <div>
    <div className="text-lg font-medium text-gray-800">{title}</div>
    <div className="ff-text">{detail}</div>
  </div>
);

export default DetailSectionDescription;
